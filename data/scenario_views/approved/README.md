# Approved scenario views

Only deterministic, source-backed views that pass the Scenario V5 evidence
contract may be stored here. Approval does not by itself guarantee numerical
use: point-in-time, unit, mapping, freshness, quality, and duplicate-cluster
gates are applied again at build time.
