# Proposed scenario views

Unreviewed structured report views belong here. A proposed view is never used
numerically. It must be moved through an explicit approval workflow before it
can enter `approved/`.
