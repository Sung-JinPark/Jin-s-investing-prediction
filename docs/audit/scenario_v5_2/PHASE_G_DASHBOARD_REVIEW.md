# Phase G — Dashboard contract

Gate: **PASS; SCREENSHOTS INCLUDED IN REVIEW EVIDENCE**

The repository dashboard keeps `#future` on the champion and `#future/research` on V5.2. The research chart defaults to three months and also exposes 1M/2026/2027. It uses one log scale, an actual 25/75 history/forecast coordinate split, three conditional p50 lines, scenario-specific dotted medoids, and a gray total-mixture p25-p75 reference band. The cards disclose each DB layer, zero origin overlap, three unique provenance hashes, the valuation-data block, A/B/C meanings, selected n, and path counts. Stored probabilities remain fractions; only the UI converts them to percent. October 2 is an ordinary CDF coordinate and `exact-date forecast=false`.
