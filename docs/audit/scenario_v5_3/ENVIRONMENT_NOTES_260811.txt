Environment classification log — 2026-08-11 KST

1. `uv run pytest ...` could not start because `uv` was not on PATH.
   Classification: environment/tool availability; not a code failure.

2. The Codex bundled Python runtime did not include pytest.
   Classification: environment/package availability; no dependency was installed or changed.

3. The existing system Python 3.12 environment contained the repository's declared test dependencies.
   Targeted tests were then run with `python -m pytest` without installing, removing, or upgrading any package.

4. Browser viewport override required an explicit reload before the requested 390x844 viewport became active.
   The final captures record `innerWidth=390`, `innerHeight=844` and document-level overflow checks.
