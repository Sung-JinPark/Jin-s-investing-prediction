# Scenario V5.3 UI remediation review — 2026-08-10

## 결론

V5.3 독립 감사에서 고객 화면에 유효한 제안만 채택했다. 기존 Scenario V5.2의 시나리오별 데이터 군집, 가중치, 고용 성장위험과 정책완화 분리 계산, S1 전용 닷컴 강도 `0.60`은 변경하지 않았다. 이번 변경은 연구 후보의 숫자를 다시 계산하는 모델 승격이 아니라, 사라진 연구 화면을 복구하고 동일 데이터의 의미를 정확히 전달하는 표시 계층 개선이다.

공식 스냅샷, 예측 원장, 캘리브레이션, 질문 레지스트리, 시나리오·신호·유동성·교차자산 아카이브는 수정하지 않았다.

## 감사 제안 처리

| V5.3 감사 제안 | 처리 | 이유와 구현 |
|---|---|---|
| 연구 코호트 가중치와 결과 확률을 구분 | 채택 | S1/S2/S3 값은 `연구 코호트 가중치`로 표시하고 2026·2027·ATH·−10% 확률은 별도 카드로 분리했다. |
| 세 조건부 경로를 직접 비교 가능하게 표시 | 채택 | S1/S2/S3 p50을 하나의 공통 로그축에 겹쳐 표시한다. |
| p50에 인위적인 흔들림을 넣지 않음 | 채택 | 굵은 선은 원본 p50, 점선은 각 군집의 실제 중심 모의 경로(medoid)다. |
| 기본 화면에서 단기 방향을 바로 확인 | 채택 | 기본 범위를 다음 1개월로 설정하고 3개월·2026 연말·2027 연말 버튼을 제공한다. |
| 교차자산·유동성 참고 화면의 의미를 설명 | 채택 | NASDAQ·Realty Income 실측, Bitcoin 반사실, 유동성 상대수준을 `숫자 → 뜻 → 주의점` 카드로 설명한다. |
| S1/S2/S3를 보정된 bull/base/bear 확률로 재정의 | 보류 | 독립 감사도 현재 값이 보정된 발생확률이 아니라고 지적했다. 이번 요청은 기존 군집·가중치를 보존하므로 의미를 바꾸지 않았다. |
| 닷컴 `0.60` 재튜닝 또는 S3 군집 재설계 | 보류 | 사용자가 기존 시나리오별 DB와 가중치를 건드리지 말라고 명시했다. 모델 구조 변경에는 별도 shadow validation이 필요하다. |
| FOMC 확률·이벤트 커널·의존성 구조 재추정 | 보류 | 감사에서 필요한 후속 연구이지만 UI 복구 범위를 넘어선다. 현재 후보의 `degraded` 상태를 숨기지 않는다. |

## 복구 및 개선 결과

### 1. 하나의 전망 그래프

- 세 선은 같은 시작점, 같은 로그 스케일, 같은 기간을 사용한다.
- 기본 다음 1개월 종점은 `2026-09-04`다.
- 다음 1개월 p50은 S1 `27,175.01`, S2 `26,292.48`, S3 `25,996.26`이다.
- 2026 연말 p50은 S1 `29,963.53`, S2 `28,199.58`, S3 `19,258.95`이다.
- 2027 연말 p50은 S1 `37,161.84`, S2 `34,121.14`, S3 `13,163.70`이다.
- 10월 2일은 배열 안의 월별 체크포인트일 뿐 exact-date forecast로 표시하지 않는다.

### 2. 시나리오 의미 보존

- S1 확장: 닷컴 가격 국면과 성장 지표 중심 군집.
- S2 균형: 현대 시장·거시 지표·회복 국면 중심 군집.
- S3 스트레스: 긴축·금리 부담·금융여건 악화 중심 군집.
- 연구 코호트 가중치는 S1 `79.52%`, S2 `14.70%`, S3 `5.78%`다.
- 닷컴 강도는 S1 `0.60`, S2 `0`, S3 `0`으로 그대로다.

### 3. 결과 확률과 근거

- 2026 연말 현재 기준 이상: `73.39%`.
- 2027 연말 현재 기준 이상: `82.61%`.
- 2026년 새 최고치 최초 도달: `93.44%`.
- 10월 말까지 −10% 선 최초 도달: `28.93%`.
- 고용 성장위험 효과: `−2.63%p`.
- 정책완화 효과: `+2.53%p`.
- 닷컴 상승 효과: `+4.78%p`.
- 가산 잔차: `0`.

### 4. 복구한 화면

- 과거 혁신 사이클 비교.
- NASDAQ·Bitcoin·Realty Income 자산 비교.
- Realty Income 가격과 배당 재투자 총수익 proxy.
- Fed 순유동성과 NASDAQ·Bitcoin 26주 수익률.
- 2026·2027 기간 전환.

기존 V5.2 조기 반환 때문에 접근할 수 없던 패널을 V5.2 탭 구조에 다시 연결했다. 직접 URL `#future/cross-asset`, `#future/liquidity`에서도 열린다.

## 모델 리스크 고지

현재 후보 상태는 `degraded`다. 직접 이벤트 수익률 커널에 사용할 수 있는 적격 역사 이벤트가 1건뿐이며, 선호 최소값은 60건이다. 따라서 이 화면은 연구용 조건부 전망이고 목표가격, 사건 확률의 보정 완료본, 자동매매 신호가 아니다. UI 개선으로 이 한계를 감추거나 숫자를 보정하지 않았다.

Bitcoin은 2009년 이전 실측 시장가격이 없으므로 2001~2006 구간에 현대 민감도를 적용한 반사실 참고 경로만 표시한다. 유동성과 가격의 동행 역시 인과관계로 표현하지 않는다.

## 검증

- JavaScript syntax: PASS.
- Targeted Scenario/UI tests: `54 passed in 7.61s`.
- Full regression after the final label fix: `459 passed in 99.07s`.
- Local static Pages build: PASS.
- Browser: 다음 1개월·2026·2027, 직접 교차자산 URL, 직접 유동성 URL, `NaN`/`Infinity` 부재 확인.
- Protected manifest before/after: `d2b096f95e34cbd7836e3da9988b590694e3af70e5acd3fb33abaffc5c7ae73b` / same.

## 입력 감사 패키지

- File: `AI_INVESTING_SCENARIO_V5_3_AUDIT_AND_REMEDIATION_PACK_260810.zip`
- SHA-256: `2f5f3ac44ddc1cf4af1ecf7a98cd68ff95f246e3e71f01cd106820c2b65098c7`
