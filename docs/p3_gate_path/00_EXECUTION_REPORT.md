> **게이트 상태: 미결 (0.33 SE).** 이 문서 어디에도 "통과"는 없다.
> 행 평균 0.15986 이 문턱 0.18 아래이지만 SE 여유가 0.33 이라 통계적으로 미결이고,
> 문항 등가중 0.22079 는 문턱 위다. 두 진술 다 참이며 분모가 다를 뿐이다.

# NASDAQ P3 게이트 경로 실행 팩 — 종료 보고 (2026-09-11)

브랜치 `claude/p3-gate-path` · 워크트리 `worktrees/active/v13-p4` · **푸시·PR 없음(사용자 몫)**

## 1. 커밋 목록 (9건, 기점 `822fbc75`)

| 해시 | 제목 |
|---|---|
| `4abd6dbe` | p3(T01): 게이트 표시층 이중 단위 — 행 평균과 문항 등가중 병기 |
| `9337f183` | p3(T02): 질문 포트폴리오 사전등록 계약 + 등록 집행 훅 — **결과 전 커밋** |
| `f58a88e3` | p3(T01): 얇은 표본에서 이중 단위 패널이 리포트를 죽이던 결함 수정 |
| `3c3bc43a` | p3(T02): 승인 12건 등록 — 계약 훅 12/12 통과, 레지스트리 67→79 |
| `7fc4bc38` | p3(T03): σ 공급 경로 — 양 일치 + 큰 σ, V13 배선은 대상 0 페일클로즈 |
| `0ed393a7` | p3(T04): NFP 예약 손실을 결과 전에 기록 + 손실 3분해 |
| `9ad0ad69` | p3(T05): 소실률 정본 유지 · lite 은퇴 · 꺼져 있던 가드 두 개 복구 |
| `2f42305b` | p3(T06): 정지 규칙·부정 선언 코드화 + 금지 행위 감지기 + 배지 문구 |
| `95454428` | p3(T07): 발굴 엔진 · 사구간 감시 · 추론 코어 v1.1 + 게이트 이후 로드맵(문서만) |

## 2. 태스크별 수용 기준 충족표

| 태스크 | 수용 기준 | 결과 |
|---|---|---|
| **T01** | 이중 단위 병기 · 게이트 산술 무변경 · 판정 단어 금지 | **충족** — 행 0.15986 / 문항 0.22079 / SE 0.0617 / CI90 [0.06894, 0.31583] · 복수 회차가 primary 행의 50% 상시 노출 · 테스트 11 |
| **T02** | yaml 커밋 해시 존재 · CLI 거부 테스트 · 신규 12건 목록 + 탈락 8건 사유 파일 | **충족** — 계약 `9337f183`(결과 전) · 훅 테스트 13 · `SELECTION.md`·`REGISTRATION.md` · 레지스트리 67→79 · 훅 12/12 통과 |
| **T03** | σ·출처 100% 기재 · 확률 결합 0 · 배선은 검증 셀 한정 | **충족** — 12/12 에 `sigma_quantity`·`sigma_path` · V13 σ 공급 0 · 배선 대상 0(페일클로즈) · 테스트 22 |
| **T04** | r1 무수정 · r2 시각 증명 · 분해 리포트 · 향후 z 게이트 | **부분** — r1·원장 무수정 ✓, 분해 리포트 ✓(커버리지 0), z 게이트 ✓. **r2 는 실행하지 않았다**(§4) |
| **T05** | asml·mu 수정 커밋 · 18건 캘린더 · 소실률 정본 결정 · 티어 단가 실측표 | **부분** — mu 마감 기재 ✓, 31건 캘린더 ✓, 소실률 정본 ✓, 단가표 ✓. **asml r2 는 실행하지 않았다**(§4) |
| **T06** | 감지기 3케이스 테스트 · 20/30 리포트 드라이런 · 배지 문구 | **충족** — 감지기 3케이스 + 추가 허용 + 보류≠통과 · 리포트 드라이런 ✓ · 배지에서 "통과" 제거 · 테스트 15 |
| **T07** | 항목별 설계문서 · 코드는 1~3만 · 4는 문서 · ML 게이트 저촉 0 | **충족** — 코드 3종(발굴·감시·v1.1) · 4~6 문서 · ML 게이트 저촉 0 · 테스트 18 |

**공통 불변식 전건 충족**: `forecasts/` diff 0 · `calibration/` diff 0 · 게이트 SQL diff 0 ·
사후 failed 재분류 0 · ML 게이트 준수 · extremization 0 · 홀드아웃/봉인 미열람 · 시크릿 미로드 ·
push·PR 0. `tools/gate_review.py --guard-only` 위반 0건.

## 3. 팩의 전제 중 실측과 어긋난 것 (정정 4건)

| 팩 | 실측 | 어디서 |
|---|---|---|
| "변동성 질문 → V13 PB 셀에서 σ" | **V13 은 σ 를 공급할 수 없다** — 셀 산출은 `P(사건)`이지 산포가 아니다. 확률에서 역산하면 z 규율이 `z = Φ⁻¹(1−p)` 항등식이 되어 자동 통과한다 | T03 |
| "L3 규칙에 따라 NFP r2 재예측" | **L3 는 2026-09-10 이후 신규 등록분 전용**이고 oct·nov 는 2026-07-20 등록이라 대상이 아니다. 실무 결과는 같다(cadence 가 이미 `r1 + D-3`) | T04 |
| void율 14.9% · 처리량 소실 6.0% | **12.7%(10/79) · 11.1%(1/9)**. 14.9% 는 계약의 *사전등록 기대값*이지 실측이 아니었다. void 10건은 **전부 예측 0건** — 구조적 소실은 **0.0%** | T05 |
| lite 단가 $0.27 | **$0.2129**(n=4). 그리고 lite 4건은 전부 `openai:gpt-5.6-terra`, 비교군 4건은 전부 `anthropic:claude-opus-4-8` 라 **티어와 제공자를 분리할 수 없다** | T05 |

## 4. 사용자 결정 대기 · 사용자 실행 대기

### 4-1. 실행 대기 2건 — 하드 라인 "시크릿 미로드" 때문에 내가 돌리지 않았다

`orchestrator.run_forecast` 는 `dry_run` 이어도 프리플라이트 안에서 `config.get_api_key()` 를
호출한다. 시크릿 이전 단계 검사는 전부 통과 확인했다.

| 항목 | 상태 | 명령 |
|---|---|---|
| `asml-eps-beat-2026q3` r2 (사용자 승인: r2 생산) | active · fixed 2026-10-14 · 등록필터 OK · 유효 티어 standard | `cd src && python -m ai_fc forecast asml-eps-beat-2026q3` |
| 신규 12건 r1 | 등록 완료, 예측 0건. T05 캘린더 W1~W8 | `cd src && python -m ai_fc forecast <question-id>` |

**비용**: 미예측 활성 31건 × 실측 $1.7197 ≈ **$53.3**(약 2.4개월치 월 sub-cap).
자동 배치로 태우지 않았다.

### 4-2. 결정 대기 3건

| # | 결정 | 왜 지금 정하지 않았나 |
|---|---|---|
| **D1** | 표준 티어 단가가 설계서 목표 $2.5~4 로 실현되면 **주 3건이 월 sub-cap $25 를 넘는다**($32.5~52). 주 2건으로 내릴 것인가, sub-cap 을 올릴 것인가 | 실측이 $1.7197(n=4)뿐이라 근거가 얇다. **첫 5회차 실측 뒤** 재판정하기로 했다 |
| **D2** | `SELECTION.md` §5-1 의 정직확률 교정 3건(`vix-40-touch` 0.08→0.13 · `t10y2y` 0.06→0.09 · `fedfunds-450` 0.07→0.04)을 채택할 것인가 | 승인 목록 밖이었고 어느 값이든 채택/거부 판정이 안 바뀐다. 등록값은 두고 `prereg.reference_class_correction` 에 **병기**만 했다 |
| **D3** | `mu-margin-qoq-fq1fy27` 의 마감 2026-12-31 은 **[미검증] 상한**이다. D-30(2026-12-01)에 Micron IR 로 실제 발표일을 확인해야 한다 | 1차 출처 조회가 이 세션 범위 밖 |

## 5. OPEN_QUESTIONS

**Q1 — 문항 등가중이 문턱 위(0.22079)인 상태가 무엇을 뜻하는가.**
게이트 정본은 행 평균이고 그 값은 0.15986 이다. 그러나 문항 등가중이 위라는 것은
**적은 수의 쉬운 문항이 여러 회차로 행을 채우고 있다**는 뜻이다(현재 복수 회차가 primary 행의
50%). 신규 12건이 해소되면 두 값이 가까워져야 한다. 가까워지지 않으면 그 자체가 신호다.

**Q2 — 손실 3분해의 커버리지가 0 인 상태를 언제 벗어나는가.**
해소 10행 전부 분해 불가다(등록 시점 정직 확률이 기록된 문항이 아직 해소되지 않았다).
활성 61 중 25 가 분해 가능하므로 **첫 해소가 나와야** 기대 바닥 / 뽑기 / 앵커 초과의
비중을 처음 볼 수 있다. 그 비중이 재도전 설계를 정한다.

**Q3 — NFP 2건의 상관을 게이트 분모가 어떻게 셀 것인가.**
`nfp-oct2026`·`nfp-nov2026` 은 같은 날 같은 전제로 66% 가 나왔다. DISTINCT 로는 2문항이지만
독립 표본이 아니다. 문항 클러스터 부트스트랩은 **같은 질문의 회차**만 묶고 **다른 질문의
공통 전제**는 묶지 않는다. 이 상관을 CI 에 반영할 방법이 지금 없다.

**Q4 — 사구간 감시가 못 보는 36문항.**
정직 확률이 기록되지 않아 판정 자체가 불가능하다. 소급 기재는 "결과를 본 뒤 정직 확률을
정하는 것"이 되어 금지된다. **이 36건은 영구히 감시 밖**이며, 커버리지는 새 등록으로만 는다.

**Q5 — lite 은퇴 근거의 교락을 언제 풀 것인가.**
lite 4건 = openai, 비교군 4건 = anthropic 이라 티어와 제공자가 분리되지 않는다.
푸는 방법은 anthropic 에서 lite 를 몇 회차 돌려 보는 것인데, **그 회차가 게이트 표본에
들어간다**. 실험을 위해 표본을 오염시킬 수는 없다. 이 교락은 **풀지 않은 채로 둔다**.

**Q6 — 추론 코어 v1.1 의 효과를 언제 물을 것인가.**
`prompt_version` 이 v1 코호트와 v1.1 코호트를 구별한다. 그러나 코호트 비교는 표본이 쌓인
뒤의 질문이고, **지금 답하면 그것이 곧 사후 서사**다. 물을 시점을 미리 정해두지 않았다.

## 6. 이번 실행이 실제로 바꾼 것

숫자로 바뀐 것은 하나다 — **레지스트리 67 → 79 문항**. 나머지는 전부
**나중에 조용히 틀리는 것을 지금 시끄럽게 만든 장치**다.

- 게이트 Brier 가 단위 의존 진술임을 상시 노출한다 (T01)
- 등록 시점이 유일한 정직한 개입 지점임을 계약과 훅으로 고정했다 (T02)
- σ 를 기계가 공급하되 **임계는 사람이 정한다**는 경계를 코드에 박았다 (T03)
- 예약된 손실의 크기를 **결과 보기 전에** 기록했다 (T04)
- 꺼져 있던 가드 두 개를 켰다 — 36문항이 컷오프 검사를 **면제**받고 있었다 (T05)
- 금지 행위 세 가지를 CI 에서 잡는다 (T06)
- 사구간 경고가 **극단화 유도로 읽히지 않도록** 금지 문구를 함께 넣었다 (T07)
