> **공통 불변식(모든 태스크)** — forecasts/ 무수정 · calibration/ledger.csv append-only · 게이트 산술(DISTINCT 50 ∧ AVG(brier)<0.18, SQL) 한 글자 무변경 · 사후 failed 재분류 금지(즉시 중단 사유) · ML 게이트(해소 100+ isotonic/Platt, 200+ 가중결합) 준수 · extremization 금지 · 공식 확률 = LLM rN(8-8) · 결과 본 뒤 규칙·문턱 교체 금지 · 홀드아웃/봉인 미열람. 각 태스크는 브랜치 신설, 로컬 커밋, push 금지, 종료 시 §보고 형식.

# T05 — 재고 위생 · 처리량 · 품질 티어

## 항목
1. `asml` 유일 회차가 failed → 정상 회차 1건 복구(새 r2, 원장 규칙대로). 2. `mu-margin-qoq-fq1fy27` 마감 null → CLI 거부 원인 수정 후 마감 기재. 3. 미예측 활성 18건 스케줄(마감 역순, 주 3건). 4. **소실률 정의 확정**: void율 14.9%를 정본(구조적 소실), 처리량 소실 6.0%는 참고 — GATE_ARITHMETIC 시나리오 재계산(기대 47.8 → 신규 12건 시 58.0). 5. **품질 티어**: lite($0.27)는 failed/degraded 4/4 → 게이트급 회차 티어(리서치 깊이 하한 L4 충족) 단가 실측 5건 후 Q12 재판정. 월 sub-cap $25 내 주 3건 성립 여부 표.

## 수용 기준
asml·mu 수정 커밋 · 18건 캘린더 · 소실률 정본 결정 기록 · 티어 단가 실측표.
