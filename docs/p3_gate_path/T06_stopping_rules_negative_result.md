> **공통 불변식(모든 태스크)** — forecasts/ 무수정 · calibration/ledger.csv append-only · 게이트 산술(DISTINCT 50 ∧ AVG(brier)<0.18, SQL) 한 글자 무변경 · 사후 failed 재분류 금지(즉시 중단 사유) · ML 게이트(해소 100+ isotonic/Platt, 200+ 가중결합) 준수 · extremization 금지 · 공식 확률 = LLM rN(8-8) · 결과 본 뒤 규칙·문턱 교체 금지 · 홀드아웃/봉인 미열람. 각 태스크는 브랜치 신설, 로컬 커밋, push 금지, 종료 시 §보고 형식.

# T06 — 정지 규칙·부정결과 선언 절차 코드화

## 절차
1. `tools/gate_review.py`: 해소 문항 20·30 도달 시 자동 중간검토 리포트(행 평균·문항 등가중·클러스터 CI90·도메인 분해·예약 손실 시나리오).
2. 문항 30에서 CI90 하한 >0.20 → `NEGATIVE_RESULT_DECLARED` 상태 + 템플릿 문서 생성(원인 분해·재도전 조건=새 포트폴리오 계약).
3. 금지 행위 감지기: 게이트 SQL 변경·failed 태그 사후 변경·원장 행 삭제 → CI 실패.
4. 대시보드에 "게이트 상태: 미결(0.33 SE)" 배지 — "통과" 표현 금지.

## 수용 기준
감지기 3케이스 테스트 · 20/30 리포트 드라이런 · 배지 문구.
