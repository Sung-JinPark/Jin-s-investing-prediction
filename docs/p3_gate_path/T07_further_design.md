> **공통 불변식(모든 태스크)** — forecasts/ 무수정 · calibration/ledger.csv append-only · 게이트 산술(DISTINCT 50 ∧ AVG(brier)<0.18, SQL) 한 글자 무변경 · 사후 failed 재분류 금지(즉시 중단 사유) · ML 게이트(해소 100+ isotonic/Platt, 200+ 가중결합) 준수 · extremization 금지 · 공식 확률 = LLM rN(8-8) · 결과 본 뒤 규칙·문턱 교체 금지 · 홀드아웃/봉인 미열람. 각 태스크는 브랜치 신설, 로컬 커밋, push 금지, 종료 시 §보고 형식.

# T07 — 더 나아간 설계 (ML 게이트 이전에 합법인 것만)

1. **질문 발굴 엔진(발행자 예보형)**: 기업 가이던스·정례 통계 발표·FOMC SEP처럼 "피예측 대상이 스스로 예보를 내는" 질문을 자동 후보화 → z 규율로 여과. earnings 우월성(0.0282)의 이식.
2. **사구간 감시**: 등록·예측 시 p∈[23.5%,76.5%]·z<0.7 경고. 예측을 바꾸지 말고 **질문을 바꾼다**.
3. **추론 코어 v1.1**(가중 학습 없음): 프리모템 체크리스트에 "전제 개정 취약성"·"컨센 존재 여부"·"임계 z" 3항 필수화. 오픈웨이트는 추론 전용.
4. **게이트 이후 로드맵 문서**: 해소 100+ 시 isotonic(cross-fit) · 200+ 시 결합 가중 — 지금은 설계만, 코드 금지.
5. **트랙 정합**: V13 검증 셀 base rate와 LLM 확률의 괴리 칩(표시 전용, 자동 재예측 0) 유지 — edge 활성화는 P3 후.
6. **연간 계획**: 12건 신규 + 18건 소화 + 회차 규율로 2027-01 중 50문항 · 문항 등가중 Brier도 0.18 하회를 목표로 병기(정본은 행 평균).

## 수용 기준
각 항목 설계문서 1개 + 코드는 1~3만 · 4는 문서 · ML 게이트 저촉 0.
