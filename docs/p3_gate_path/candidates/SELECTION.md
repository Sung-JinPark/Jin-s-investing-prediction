# T02 — 신규 12건 선택과 탈락 8건 사유 (2026-09-10)

> 계약 `questions/portfolio_prereg_v1.yaml` (커밋 `9337f183`, 결과 전 등록) 의 z 규율을 적용한 결과다.
> ~~**아직 등록하지 않았다**~~ — 이 문서는 **선택 시점**의 기록이다. 2026-09-10 사용자 승인
> (A · 12건 전부) 뒤 12건을 `questions/registry.yaml` 에 등록했다 → [`REGISTRATION.md`](REGISTRATION.md).
> `forecasts/`·`calibration/` 은 등록 후에도 무수정이다(예측 0건).

## 0. 검증이 확인한 것

후보 20건의 z 를 하나씩 재계산했다. **산술 오류 0건** — 모든 z 가 `|임계−중심|/σ` 로 소수 둘째 자리까지 재현된다.
실패는 전부 **σ 의 의미론**에서 났다(출처 부재·다른 양을 잼·레짐 혼용·지평 불일치).

| 분류 | 건수 |
|---|---|
| 채택 가능 | 9 |
| 예외 슬롯 필요 | 3 |
| 거부 | 8 |

## 1. 채택 가능 9건

| id | 도메인 | 마감 | 정직확률 | 기대 Brier 바닥 | σ 출처 |
|---|---|---|---|---|---|
| `adbe-eps-surprise-ge4pct-fq4-2026` | earnings | 2027-01-15 | 0.04 | 0.0384 | base_rates/earnings.md ADBE 8분기 SD 0.465 |
| `nvda-rev-surprise-ge6pct-fq3-2027` | earnings | 2026-11-25 | 0.09 | 0.0819 | base_rates NVDA 9분기 SD 1.463 |
| `nvda-rev-miss-fq3-2027` | earnings | 2026-11-25 | 0.05 | 0.0475 | 동일 계열 |
| `avgo-eps-surprise-ge5pct-fq4-2026` | earnings | 2026-12-15 | 0.08 | 0.0736 | r1 4분기 + ledger 1건 SD 1.239 |
| `avgo-eps-miss-fq4-2026` | earnings | 2026-12-15 | 0.06 | 0.0564 | 동일 계열 |
| `nfp-dec2026-below-neg75k` | macro | 2027-01-09 | 0.09 | 0.0819 | base_rates/macro.md σ≈95K |
| `vix-40-touch-by-2027-01-15` | volatility | 2027-01-15 | 0.08 | 0.0736 | dualdb 조건부 87일 최고종가 n=1347 |
| `vix-above20-40days-by-2027-01-15` | volatility | 2027-01-15 | 0.11 | 0.0979 | 동일 원본 · VIX>20 일수 분포 |
| `t10y2y-100bp-by-2027-01-15` | market-regime | 2027-01-15 | 0.06 | 0.0564 | dualdb T10Y2Y 최댓값 n=2154 |

## 2. 예외 슬롯 필요 3건 — 슬롯 3개를 전부 소모한다

| id | 실효 z | 사유 |
|---|---|---|
| `adbe-eps-above-guide-top-fq4-2026` | 1.040 | σ 출처는 실재(base_rates 'Adobe 가이던스 상단도 4/4 초과: +$0.11/+0.10/+0.16/+0.11'), sd=0.02708·평균 0.120 으로 초안값 정확, z=0.120/0.0271=4.428 산술도 정확. 그러나 정합성이 깨진다: z=4. |
| `fedfunds-upper-450-2026-12-09` | 0.810 | σ 출처는 1차 확인됨: forecasts/2026/2026-09-09_fomc-sep-2027-below-2026_r1.md frontmatter 의 sep_2026_06_dots_2026ye 가 '4.375×1 · 4.125×5 · 3.875×3 · 3.625×8  |
| `nasdaq-comp-30000-close-2027-01-15` | 0.950 | σ 출처는 완전 검증됐다 — multivariate_v8_latest.json 의 horizons.63 p25=−0.010639911·p75=+0.0825412963 이 초안 인용값과 정확히 일치하고, IQR/1.349×√(87/63)=0.081166 도 재현된다. 저 |

## 3. 거부 8건 — 비선택 기록

선택되지 않은 것도 기록한다. 나중에 '왜 이건 안 골랐나'를 다투지 않기 위해서다.

| id | 실효 z | 거부 사유 |
|---|---|---|
| `mu-gm-qoq-drop10pp-fq1-2027` | — | σ 수치 자체는 재현된다(base_rates 12분기 GM 계열 → QoQ 델타 11개, 표본SD=6.4572·평균=8.6818). 문제는 그 σ가 묻는 사건을 측정하지 않는다는 것이다: 델타 11개 중 10개가 양수이고 최소값이 −1.6%p 이며, 수준이 −10.9%→84.6% 로 단조 상승한 업사이클 전용 표본이다. 질문의 임계는 −10%p 하방인데 표 |
| `orcl-eps-miss4pct-fq2-2027` | 0.800 | z 산술은 맞다(표본SD=4.1475·평균=2.1200, z=|−4.00−2.120|/4.1475=1.475). 그러나 σ 가 단일 배제 결정에 걸려 있고 그 결정이 밴드를 가른다. r1 파일 94행이 'FY26 Q2 의 +38.0% 서프라이즈는 이상치로 판단해 기저율에서 별도 취급'이라 명기하는데, 그 값을 포함하면 평균 8.100·SD 15.110 →  |
| `fedfunds-cut-to-350-by-2027-01-15` | — | 계약 required_fields_on_registration 이 요구하는 sigma 가 아예 없다('n/a(확률직접)'). 그리고 z=1.75 는 Φ⁻¹(1−0.04) 로 정직확률에서 역산한 값이다 — z 규율의 존재 이유는 p 를 독립적으로 구속하는 것인데, p 에서 z 를 뽑으면 항등식이 되어 어떤 p 를 써도 자동 통과한다. 등록을 막는 장치가 통과 |
| `unrate-dec2026-below-38` | 1.000 | 초안 스스로 σ 를 [부분 미확인]으로 표기하고 등록을 차단해 뒀다 — 규율 준수 자체는 모범적이다. 그러나 판정은 현 상태 기준이어야 하므로 거부다. 0.20%p 는 BLS 기술노트의 '월간 변화 90% CI ±0.2%p'(→표본오차 σ₁≈0.12%p)에 실질 변화분을 얹은 파생값이고 1차 출처가 없다. z=|3.85−4.1|/0.20=1.250 산술은  |
| `cpi-dec2026-core-below-21` | 0.250 | 중심추정이 지평과 어긋난다 — 이 세트에서 가장 큰 산술 결함이다. 중심 2.4% 는 8월분 값이다(2026-09-09_cpi-aug2026-core-below-24_r1.md frontmatter 의 consensus_core_yoy '2.4%'와 cleveland_fed_nowcast 'YoY 2.38%'로 확인). 그런데 질문은 12월분이고, 초안은  |
| `gdp-q3adv-2026-contraction` | 1.710 | 상호배타인 두 질문(M6·M7)에 같은 변수의 서로 다른 중심(1.5 와 2.5)을 각각 배정해 양쪽 다 z=1.28 을 만들었다. 한 확률변수에 중심은 하나뿐이므로 최소한 한쪽 z 는 틀렸고, p=0.09 두 개도 동시에 성립할 수 없다. 단일 중심 c 로 재계산하면 c=2.0 에서 z_M6=z_M7=1.71(p 각 0.044), c=1.5 에서 z_M6 |
| `gdp-q3adv-2026-boom` | 1.710 | M6 와 동일 사유 — 같은 릴리스·같은 변수인데 중심만 2.5 로 바꿔 z=1.28 을 만들었다. σ 출처(GDPNow RMSE 1.17%p)는 macro.md 에서 1차 확인되나 중심이 GDPNow 가 아니라 가정값이라 σ 와 중심의 출처가 불일치한다. FRED 2차 출처의 vintage 문제도 동일(ALFRED 명시 필요). 구조 자체는 이 세트에서  |
| `pskd-wbd-close-by-2027-01-15` | — | σ 가 존재하지 않는다. 초안은 정직하게 [미확인]으로 표기했고 '이 값을 채택 근거로 쓰지 말 것'이라고까지 적었다 — 그 정직성은 인정하지만, 그렇다면 계약의 required_fields(z·sigma) 를 충족하지 못하고 z=1.51 은 가정 σ=45일에서 나온 대리값, 즉 지어낸 σ 다. 판정은 거부여야 한다. 추가로 사실관계 전량(2026-02-2 |

## 4. 포트폴리오 산술 — 문항 수 자체가 방어다

| 안 | 건수 | 예외슬롯 | 기대 바닥 | 1건 YES | 2건 YES |
|---|---|---|---|---|---|
| **A** 채택9 + 예외3 | 12 | 3 | 0.0784 | 0.0857 | **0.1607** |
| **B** 채택9만 | 9 | 0 | 0.0675 | 0.1080 | **0.2080** |
| **C** 상관쌍 정리 6 + 예외3 | 9 | 3 | 0.0849 | 0.1129 | **0.2107** |

문턱 0.18 · 현 primary 행 평균 0.15986(10행).

**반직관적 결과**: B 가 기대 바닥은 가장 좋지만(0.0675) 꼬리에 가장 약하다. 2건이 터지면 0.2080 으로
문턱을 넘는다. A 는 바닥이 조금 높아도 **분모가 손실을 희석**해 2건 YES 에서도 0.1607 로 버틴다.
즉 이 설계에서 **문항 수는 비용이 아니라 보험**이다.

## 5. 검증이 남긴 경고 3가지

1. **19/20 이 저확률 NO 베팅**이다(평균 정직확률 0.09). 한 건이 YES 로 터지면 그 행 Brier 가 0.81~0.86 이다.
   지금 필요한 것은 z 를 더 키우는 것이 아니라 **정직 확률을 참조클래스 위로 되돌리는 것**이다 —
   권고 교정: `vix-40-touch` 0.08→0.13 · `t10y2y-100bp` 0.06→0.09 · `fedfunds-upper-450` 0.07→0.04.
2. **상관쌍 3개**가 유효표본을 깎는다: nvda 2건·avgo 2건은 같은 발표의 같은 컨센 스냅샷을 공유해 **동시 void**
   되고, vix 2건은 같은 vol-regime 드라이버를 탄다. 게이트 DISTINCT 로는 6문항이지만 유효표본은 그보다 작다.
3. **기등록 중복 1건**: `mu-margin-qoq-fq1fy27`(active)이 이미 있어 `mu-gm-qoq-drop10pp-fq1-2027` 은 그 진부분집합이다.
   또 `fedfunds-upper-450` 의 p=0.07 은 기등록 `fedfunds-upper-425-2026-12-09`(예상 12%)와 P(3회째|2회)=58% 를 함의해 부정합이다.

## 6. 하지 않은 것

> **2026-09-10 갱신 — 이 절의 첫 항목은 더 이상 사실이 아니다.** 사용자 승인(A · 12건 전부) 뒤
> 12건을 `questions/registry.yaml` 에 등록했다(67 → 79 문항). 등록 기록·정본 선택 규칙·훅 판정은
> [`REGISTRATION.md`](REGISTRATION.md). 아래 원문은 승인 **이전** 시점의 기록으로 그대로 둔다.

- 질문을 등록하지 않았다. `registry.yaml` diff 0.
- `forecasts/`·`calibration/` 무수정.
- σ 를 지어내지 않았다. 출처 없는 후보는 거부로 분류했다(M2·C1m·M5).
- 성적을 보고 고르지 않았다 — 등록 판정 훅은 원장을 읽지 않는다.
