# V13-VOL rung-4 판정 — 봉인 아카이브 내 매크로 피처블록: 27검정 전부 '측정된 영' (2026-09-09)

> 사전등록 `rung4_macro_blocks`(결과 전 커밋 **7a868424**) → 실행 → `data/timeseries_v13/vol/ladder_rung4_macro.json`
> → 원장 `V13VOL_rung4_macro`. 예산 **4/8**. 설계창(2007-2014)만 열람 — 홀드아웃·봉인 미열람, 새 창 0.
> 새 데이터 적재 0 · 비용 $0 · 라이선스 위험 0(봉인 V2 아카이브 보유 계열만).

## 0. 판정

**채택 0/27.** 세 블록 전부, 아홉 셀 전부에서 당일 수준 단독 기준선(PB)을 양방향으로 이기지 못했다.
family P(k≥0 | n=27) = 1.0000. **적재 없이 시험 가능한 마지막 정보집합이 비어 있었다.**

## 1. 시험한 것

| 블록 | 정의 | PIT 등급 | 지연 |
|---|---|---|---|
| F1 기간스프레드 | DGS10 − DGS2 (수준) | **archive_verified** — 과거 행의 `available_at`이 당일 22:00 UTC(H.15 발행 후)로 실측 확인 | 0세션 |
| F2 달러 | DTWEXBGS 21세션 로그변화율 | assumed_lag — `available_at`이 2026 수집 시각이라 PIT 증거 없음 | 10세션 |
| F3 EBP | FED_EBP 수준(월간) | assumed_lag — 같은 이유 + 월간 발행 | 42세션(≈2개월) |

후보 = `logit[PB 피처 + β·F_k]`, 피처당 파라미터 1개 추가. 기준선은 현 champion인 PB.
셀은 **9개 전부** — 홀드아웃 결과로 셀을 고르면 그 정보가 새 설계창 시험에 들어가 다음 홀드아웃이 오염된다.

## 2. 결과 분포

| 판정 | 건수 |
|---|---|
| measured_zero (양방향 CI90 하한 > 0 실패) | **27** |
| null_leak (CI 통과 후 귀무 실패) | 0 |
| adopted | **0** |

건전 귀무는 한 건도 실행되지 않았다 — CI 단계에서 전부 걸렸기 때문이다(설계상 CI 통과 셀에만 귀무를 돌린다).

**가장 나았던 한 방향**조차 반대 방향에서 무너진다.

| 조합 | early→late | late→early |
|---|---|---|
| vix30_h63 \| F1 기간스프레드 | **+0.03001** [+0.01025,+0.04735] | **−0.10196** [−0.16075,−0.04668] |
| rv_h63 \| F3 EBP | +0.00186 [−0.00232,+0.00669] | +0.00414 [−0.00169,+0.01053] |

첫 행이 이 캠페인의 전형적 실패 모양이다: 전반창에서 유의하게 좋고 후반창에서 **세 배 크게 유의하게 나쁘다**.
한 방향만 봤다면 "기간스프레드가 3개월 VIX 30 터치를 예측한다"고 결론 냈을 것이다. 양방향 전이 요구가
정확히 이것을 막는다.

**가장 크게 악화된 조합**은 전부 EBP다: vix25_h21 −0.2135 · vix25_h63 −0.1991 · vix30_h63 −0.1213(l2e).
월간·2개월 지연 계열을 일간 사건 라벨에 붙이면 계단 함수가 되어 파라미터 하나가 순수 잡음으로 작동한다.

## 3. 이 결과의 의미

1. **V9의 무효익이 표적을 바꿔도 유지된다.** V9는 같은 계열들을 *가격 분포(CRPS)* 표적에서 시험해 전부 무효였다.
   여기서는 표적과 손실함수를 모두 바꿨는데(사건 확률·Brier) 결과가 같다. 두 표적에서 독립적으로 영이 나온 것은
   "이 계열들에 NASDAQ 관련 증분 정보가 거의 없다"는 판단을 강화한다.
2. **무료 경로가 소진됐다.** 봉인 아카이브 7계열 중 표적·기준선에 쓰이지 않은 것은 이 셋뿐이었다. 이제 남은
   정보집합은 **전부 외부·유료**다(옵션 표면, 일중). 즉 P5 이후의 진전은 데이터 구매·라이선스 회신에 종속된다.
3. **PB의 지위가 더 굳었다.** rung-2(HAR) 0/9, rung-3(EWMA) 0/9, rung-4(매크로) 0/27. 세 번의 서로 다른 확장이
   전부 당일 수준 하나를 이기지 못했다. "변동성 사건 확률은 당일 변동성 수준이 거의 다 말한다"가 이 트랙의 결론이다.

## 4. 무엇을 하지 않았나 (정직)

- 결과를 본 뒤 블록·지연·임계를 바꾸지 않았다. 사전등록(7a868424)이 결과보다 먼저다.
- 채택이 0이므로 champion 재판정·재동결·홀드아웃 추가 소모는 **없다**. 슬롯 2/3 그대로.
- F2·F3의 보수 지연은 PIT 증거가 아니라 가정이다. 채택이 나왔다면 PIT 확인이 배선의 선행 조건이었을 것이나,
  채택이 0이라 그 부담은 발생하지 않았다. 지연 로직의 미래 정보 누수는 테스트 3건으로 고정했다.

## 5. 자원

| 항목 | 상태 |
|---|---|
| 개발 예산 | **4/8** (rung-1·2·3·4) |
| 홀드아웃 슬롯 | 1/3 소모 (잔여 2) |
| 봉인 | 1/1 미사용 |
| 새 창 개방 | 0 |

## 6. 다음

설계도 §3 기준 P4 종료. 남은 단계는 **P5(외부 정보집합 — CBOE 회신·구매 종속)** 와 **P6(라이브 전진 축적 — 시간)** 이며,
둘 다 지금 코드로 밀어붙일 수 있는 것이 아니다. P6는 자동으로 쌓이고(워크플로 복구 완료), P5는 사용자의 발송·구매 결정에 달렸다.
