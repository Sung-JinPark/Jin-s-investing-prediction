# R6 SHARPEN · TILT calibration checkpoint

## Boundary

This checkpoint uses only the frozen R5 calibration role, registered market history,
and registered calibration-audit artifacts. The R5 outer path and every outer role are
blocked before open. No outer row or outer numerical result is an optimization target,
selection input, or justification in this work.

The state is `WAIT_USER_DECISIONS`. It is not a terminal state, Research Gate PASS,
customer publication, or champion promotion.

## Completed without a user decision

- Append-only v1.1 registration added four previously omitted C3 audit inputs before
  any R6 candidate output was inspected. The frozen candidates, grids, roles, purge,
  embargo, comparator, and probability space are unchanged.
- Five temporal folds were built with two warm-up folds. Every evaluated fold trains
  on prior calibration origins only and excludes 63 purge plus 5 embargo origins.
- C1 variance scaling and C2 monotone empirical PIT recalibration were fitted and
  evaluated on cross-fit holdouts. The provisional calibration-only choice is C2 at
  h1, C1 at h5, and C0 at h21/h63 under the preregistered coverage-error objective
  and pinball non-degradation constraint.
- C3 found that the standardized residual pool is broadly near unit scale, except the
  longest horizon is just outside the preregistered diagnostic tolerance. Component
  width proxies cannot identify the E0-floor contribution causally without a newly
  approved sample-level reblend.
- T0 reproduced a constant direction classification in calibration. T2's return-sign
  residual tilt and T3's distribution-implied P(up) were evaluated, but neither creates
  a validated direction fix at this checkpoint. T1 remains disabled.
- Exact E0 nesting was reproduced for C1 scale=1, C2 identity mapping, and T2
  shrinkage=0 at every registered coordinate.
- Protected files are unchanged and all generated probabilities remain fractions in
  `[0,1]`.

## Decisions required before prospective shadow initialization

1. `R6-D1`: maintain the E0 floor, or authorize a formal lower-floor contract revision.
   The current audit does not prove causal E0-floor responsibility, so maintaining it
   is the conservative recommendation.
2. `R6-D2`: approve VIXCLS under DV-1 so T1 can be researched, or defer T1. Because
   T2/T3 did not resolve the direction classification, approval is the research-forward
   recommendation; it does not authorize publication or a Gate PASS claim.
3. `R6-D3`: preregister the prospective decision sample and coverage tolerance. The
   conservative recommendation is a nine-month first review for h1/h5, a ±5 percentage
   point coverage tolerance, and no h21/h63 decision before their own minimum mature
   sample is reached.

The shadow contract and append-only prospective ledger must not be initialized until
all three decisions are recorded.
